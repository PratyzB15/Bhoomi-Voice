import { LandingClient } from '@/components/LandingClient';

export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center p-4 bg-[#F4F7F2]">
      <LandingClient />
    </main>
  );
}